// Minimal assert-based test runner (no external test framework dependency).
#include <cassert>
#include <cstdio>
#include "solution.h"

int main() {
    // TODO: add real assertions, e.g. assert(solve() == expected);
    std::printf("TODO: add test cases\n");
    return 0;
}
